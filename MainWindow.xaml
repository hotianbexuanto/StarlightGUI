<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="StarlightGUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Starlight GUI">
   

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 自定义标题栏 -->
        <Grid x:Name="AppTitleBar"
              Background="{StaticResource WindowBackgroundBrush}"
              Height="36">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 应用图标和标题 -->
            <StackPanel Grid.Column="0" 
                       Orientation="Horizontal" 
                       HorizontalAlignment="Left"
                       Margin="16,0,0,0"
                       VerticalAlignment="Center">
                <FontIcon Glyph="&#xE713;" 
                          FontFamily="Segoe Fluent Icons"
                          FontSize="16"
                          Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
                          VerticalAlignment="Center"/>
                <TextBlock Text="Starlight GUI" 
                           FontSize="14"
                           Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
                           Margin="20,0,0,0"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <!-- 标题栏拖拽区域 -->
            <Grid Grid.Column="1" 
                  x:Name="DragRegion"
                  Background="Transparent"/>

            <!-- 窗口控制按钮 -->
            <StackPanel Grid.Column="2" 
                       Orientation="Horizontal"
                       HorizontalAlignment="Right">
                <Button x:Name="MinimizeButton"
                        Click="MinimizeButton_Click"
                        Style="{StaticResource CaptionButtonStyle}">
                    <TextBlock x:Name="MinimizeButtonContent"
                              Text="&#xE921;"
                              FontFamily="Segoe Fluent Icons"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
                </Button>
                <Button x:Name="MaximizeButton"
                        Click="MaximizeButton_Click"
                        Style="{StaticResource CaptionButtonStyle}">
                    <TextBlock x:Name="MaximizeButtonContent"
                              Text="&#xE922;"
                              FontFamily="Segoe Fluent Icons"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
                </Button>
                <Button x:Name="CloseButton"
                        Click="CloseButton_Click"
                        Style="{StaticResource CloseCaptionButtonStyle}">
                    <TextBlock x:Name="CloseButtonContent"
                              Text="&#xE8BB;"
                              FontFamily="Segoe Fluent Icons"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- 侧边栏区域 -->
        <controls:NavigationView Grid.Row="1"
                                 x:Name="RootNavigation"
                                 PaneDisplayMode="LeftCompact"
                                 IsBackButtonVisible="Collapsed"
                                 IsSettingsVisible="True"
                                 IsPaneToggleButtonVisible="True"
                                 IsPaneOpen="False"
                                 CompactModeThresholdWidth="0"
                                 ExpandedModeThresholdWidth="0"
                                 OpenPaneLength="280"
                                 ItemInvoked="RootNavigation_ItemInvoked"
                                 Background="{StaticResource WindowBackgroundBrush}">

            <!-- 选项，从上到下 -->
            <controls:NavigationView.MenuItems>
                <controls:NavigationViewItem Content="主页" Icon="Home"/>
                <controls:NavigationViewItem Content="进程管理" Icon="Admin"/>
                <controls:NavigationViewItem Content="系统管理" Icon="Permissions"/>
            </controls:NavigationView.MenuItems>

            <!-- 从下到上 -->
            <!-- 最下面是设置 -->
            <controls:NavigationView.FooterMenuItems>
                <controls:NavigationViewItem Content="关于" Icon="Help"/>
            </controls:NavigationView.FooterMenuItems>

            <!-- 内容 -->
            <Frame x:Name="MainFrame" Grid.Row="1"/>
        </controls:NavigationView>
    </Grid>
</Window>